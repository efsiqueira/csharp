using System;

public class SegundoExercicio
{
    public static void Main()
    {
        // Crie um programa que imprima um arrays com Foreach

        string [] vetor = { "2", "15", "6", "0", "3" };

        foreach (string valores in vetor)
        {
            Console.WriteLine(valores);
        }

    }
}